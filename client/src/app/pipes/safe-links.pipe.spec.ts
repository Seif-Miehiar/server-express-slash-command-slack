import { SafeLinksPipe } from './safe-links.pipe';

describe('SafeLinksPipe', () => {
  it('create an instance', () => {
    const pipe = new SafeLinksPipe();
    expect(pipe).toBeTruthy();
  });
});
